# تقرير: إصلاح فعلي لمشكلة عرض/تنقل الامتحان (مش شكلي)

## اعتذار قصير
التعديل اللي عملته المرة اللي فاتت كان تحسين شكلي (CSS) فقط، وطلبك كان واضح إن فيه مشكلة
حقيقية في التنقل نفسه — وكنت محق. لقيت السبب الجذري وأصلحته فعليًا.

## السبب الحقيقي (بعد فحص الكود من جديد بعمق)
صفحة الامتحان (`renderTestPage` في `app.js`) كانت مبنية بطريقة مختلفة عن **كل باقي صفحات
المنصة**: كل منطق التفاعل (اختيار الإجابة، زر Next، زر Finish، المؤقت، حساب النتيجة) كان
مكتوب جوه وسم `<script>` **مُضمّن داخل نص الـ HTML نفسه** اللي بيترجع من الدالة.

المشكلة: **المتصفح لا يُنفّذ أبدًا أي وسم `<script>` يتم إدراجه عن طريق `.innerHTML`** — ده
سلوك موثّق وثابت في كل المتصفحات (لأسباب أمنية في مواصفات الويب نفسها)، ومنصتك بتحمّل كل
صفحة بالظبط بهذه الطريقة (`content.innerHTML = renderTestPage(...)`).

يعني عمليًا: أول سؤال كان بيظهر صح (لأنه HTML ثابت)، لكن بمجرد ما الطالب يضغط على أي إجابة
أو زر Next، المتصفح كان بيحاول ينادي دالة زي `selectTestOption()` أو `testNavQ()` وهي
**غير موجودة إطلاقًا** (لأن الكود اللي بيعرّفها جوه `<script>` اللي ما اتنفذش) — فمفيش أي
حاجة كانت بتحصل، بصمت تام، من غير أي رسالة خطأ ظاهرة للطالب. ده بالظبط سبب إحساسك إن الزرار
"شكلي بس".

باقي صفحات المنصة (الدرس، الكورس، الأدمن...) شغالة صح لأنها مكتوبة بنمط مختلف: الدالة بترجع
HTML بس (من غير `<script>`)، وبعد إدراجها في الصفحة، بينادى عليها دالة تانية منفصلة اسمها
مثلاً `initLessonPage()` بتربط كل الأزرار والأحداث برمجيًا. صفحة الامتحان كانت الوحيدة الشاذة
عن هذا النمط.

## الإصلاح
نقلت كل كود التفاعل (نفسه بالظبط، بدون أي تغيير في المنطق) من جوه وسم `<script>` إلى دالة
حقيقية منفصلة اسمها `initTestPage(quizId, courseId, lessonId)`، وخليت `renderPage()` تناديها
فورًا بعد إدراج الـ HTML — **بنفس النمط المتبع في باقي صفحات المنصة تمامًا**. هذا هو التعديل
الوحيد الجوهري.

## اختبرت الإصلاح فعليًا (مش بس قراءة كود)
شغّلت الكود الحقيقي بالكامل في بيئة متصفح فعلية (jsdom) بمحاكاة كاملة لسيناريو طالب حقيقي:
دخول لاختبار من سؤالين، اختيار إجابة، ضغط Next، التأكد إن السؤال اتغيّر فعليًا، الرجوع للسؤال
الأول والتأكد إن إجابته لسه محفوظة، ثم إنهاء الاختبار. النتيجة كانت **ناجحة بالكامل**:
- ✅ سؤال واحد بس ظاهر في كل مرة (السؤال التاني كان مخفي فعليًا، مش مجرد تصميم).
- ✅ اختيار إجابة يثبتها فعليًا (`selected` بيتفعّل، نقطة السؤال في الشريط تتحول answered).
- ✅ زر Next نقل فعليًا للسؤال التالي (مش شكلي — تأكدت إن السؤال المعروض اتغيّر في الـ DOM).
- ✅ الرجوع للسؤال السابق حافظ على الإجابة المختارة (مفيش فقدان بيانات).
- ✅ الإنهاء حسب نفس نظام التصحيح الحالي: النتيجة طلعت 2/2 (100%)، نجاح، وحفظ المحاولة
  (`saveQuizAttempt`) بنفس الحقول والمنطق تمامًا من غير أي تغيير.

## الملفات اللي اتعدّلت
**`app.js` فقط** — 4 نقاط تعديل داخل نفس الملف:
1. نقل جسم الـ `<script>` بالكامل إلى دالة `initTestPage()` منفصلة (بدون تغيير أي سطر منطق).
2. استبدال القيم اللي كانت متحطة كنص جوه الـ Template (`'${quizId}'`) بمتغيرات حقيقية
   (`quizId`) بما إنها بقت باراميترات دالة فعلية — وده كمان بيصلّح ثغرة كانت ممكنة لو الـ ID
   احتوى على علامة اقتباس.
3. استدعاء `initTestPage(...)` في `renderPage()` بعد إدراج HTML مباشرة، بنفس نمط باقي الصفحات.
4. مفيش أي تغيير في منطق التصحيح، حفظ المحاولة، أو أي شيء آخر.

## ملاحظة مهمة اكتشفتها أثناء الفحص (لسه محتاجة قرار منك، ماعملتش فيها حاجة)
أثناء تتبع الكود لقيت أن ملف `data.js` فيه سطر بيحاول يستخدم متغير اسمه `COURSES_DATA` غير
معرّف في أي مكان في المشروع (يبدو أنه رصيد من نظام كورسات ثابتة قديم قبل الانتقال الكامل
لـ Firebase). هذا يوقف تنفيذ باقي ملف `data.js` من هذه النقطة فصاعدًا في كل تحميل صفحة —
لحسن الحظ باقي الكود بيتعامل مع غيابها بأمان (فيه فحوصات `typeof X === 'function'` قبل أي
استخدام)، فمفيش كسر ظاهر، لكنها لسه *خطأ حقيقي في الـ Console* في كل زيارة. **لم ألمسها
إطلاقًا** التزامًا بطلبك الصريح إن التعديل يكون على نظام عرض الامتحان بس — قولّي لو حابب
أصلّحها في مرة تانية منفصلة.
