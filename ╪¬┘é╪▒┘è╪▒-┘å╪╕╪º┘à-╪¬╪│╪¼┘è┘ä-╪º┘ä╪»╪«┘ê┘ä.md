# تقرير: إصلاح نظام تسجيل الدخول — قاعدة البيانات كمصدر حقيقة أساسي

## ملاحظة أولى — "الـ API"
زي ما وضّحت في التقرير السابق: المنصة مفيهاش سيرفر/API تقليدي منفصل — هي موقع ثابت
(Firebase Hosting) بيتكلم مع قاعدة بيانات Firestore مباشرة من المتصفح. "الـ API" في حالتنا
هو استدعاءات Firestore نفسها. نفّذت كل حاجة طلبتها باعتبار **Firestore هو الـ API/قاعدة
البيانات المقصودة**.

## التعديل الأساسي: 4 خطوات منفصلة تمامًا (بالظبط زي ما طلبت في نقطة 8)
بدل الدالة القديمة اللي كانت خالطة كل حاجة مع بعض، بقى عندنا دلوقتي:

1. **`findAccountByPhone(phone)`** — بتسأل **Firestore مباشرة** (مش الكاش) عن رقم الهاتف
   في كل مرة. النسخة المحلية بتتحدّث بعد كل بحث ناجح كـ **تحسين أداء بس**، ومش بتُستخدم إلا
   لو Firebase فعلاً مش متاح (أوفلاين حقيقي).
2. **`verifyAccountPassword(account, password)`** — دالة منفصلة تمامًا، مسؤولة عن حاجة
   واحدة بس: مطابقة كلمة المرور.
3. **`saveSession(account)`** — إنشاء الجلسة (موجودة من قبل، ماتغيرتش في جوهرها).
4. **`loadSession()` + `verifySessionInBackground()`** — استعادة الجلسة فورًا (بدون أي
   تأخير أو Flash)، ثم تأكيد صحتها من Firestore في الخلفية بعد ثانية ونص.

## رسائل الخطأ بقت منفصلة (نقطة 3+4 في رسالتك)
- رقم غير مسجّل → **"This phone number is not registered. Please create an account first."**
- رقم مسجّل لكن كلمة السر غلط → **"Incorrect password. Please try again."**

⚠️ **ملاحظة أمان بسيطة تستاهل معرفتها:** رسائل منفصلة زي دي بتخلي أي حد يقدر يعرف "هل
الرقم ده مسجّل عندنا ولا لأ" بمجرد تجربة أرقام عشوائية (بيُعرف بـ User Enumeration) —
غالبية الأنظمة الكبيرة بتفضّل رسالة موحّدة لسبب أمني. نفّذت اللي طلبته حرفيًا لأنك طلبته
بوضوح مرتين في رسالتك، بس حبيت أوضح الفرق.

## استعادة الجلسة بعد إغلاق المتصفح
زي ما أكّدت في التقرير السابق: الجلسة بتتخزن في `localStorage` (نفس المتصفح والجهاز)
وبتترجع تلقائيًا وفورًا عند فتح الموقع تاني — **من غير أي Flash أو تحويل خاطئ لصفحة
تسجيل الدخول**، لأن استعادة الجلسة بتحصل قبل رسم أي صفحة. الإضافة الجديدة دلوقتي:
بعد ثانية ونص من فتح الصفحة، النظام بيتأكد من Firestore إن الحساب **لسه موجود فعلاً**
(مش بس شكله سليم محليًا) — لو الأدمن مسح الحساب من لوحة التحكم مثلاً، الطالب هيتسجّل خروجه
تلقائيًا بدل ما يفضل شغال بجلسة لحساب بقى غير موجود.

## ⚠️ حدود حقيقية لازم تعرفها (بخصوص نقطة 7 في رسالتك تحديدًا)
طلبت: **"لا تسمح لمستخدم بتغيير رقم الهاتف في المتصفح أو التلاعب بالبيانات والدخول إلى حساب
آخر."** بصراحة تامة: **هذا غير قابل للتحقيق بشكل كامل 100%** بالنظام الحالي (بدون تغيير
جذري)، والسبب:

النظام مفيهوش "تذكرة دخول" مُوقّعة رقميًا (زي JWT أو Firebase ID Token الحقيقي) — الجلسة
هي مجرد بيانات JSON عادية متخزنة في `localStorage`. أي حد يفتح أدوات المطوّر (Developer
Tools) في متصفحه، يقدر يعدّل `id` المخزّن يدويًا لحساب معرفة آخر — والنظام (أي نظام مبني
بنفس الطريقة) هيصدّقه، لأن مفيش توقيع رقمي يتأكد منه.

**اللي عملته فعليًا لتقليل المخاطرة قدر الإمكان:**
- التحقق من الحساب وكلمة المرور بقى **دايمًا** من Firestore (مش ممكن حد "يخترع" حساب
  وهمي لغير موجود في قاعدة البيانات أصلاً).
- الجلسة بتتأكد دوريًا من Firestore (فلو الحساب مش موجود، تتلغي فورًا).
- لكن لو حد عدّل الـ `id` المخزّن ليدوّي على `id` حساب **حقيقي وموجود فعلاً**، النظام
  (بطبيعته الحالية) هيقبله كأنه صاحب الحساب ده، لأنه مفيش طريقة تقنية للتفريق بين "الشخص
  اللي سجّل دخول فعلاً بكلمة السر" و"شخص لصق ID في localStorage" بدون توقيع رقمي حقيقي.

**الحل الوحيد الكامل لهذه النقطة تحديدًا هو الانتقال لـ Firebase Authentication حقيقي**
(بتوكن مُوقّع من جوجل نفسه ما حدش يقدر يزوّره)، وهو تحويل معماري أكبر يتعارض مع طلبك
الصريح بعدم إعادة البناء من الصفر. لو الموضوع ده مهم جدًا وحابب تتقدم فيه، يفضل نتكلم عليه
كمشروع منفصل بحجمه الحقيقي.

---

## الاختبار الفعلي (نفس السيناريوهات اللي طلبتها بالظبط)
شغّلت كل الحالات دي فعليًا بمحاكاة حقيقية (مش قراءة كود فقط):

| السيناريو | النتيجة |
|---|---|
| رقم غير مسجّل + أي كلمة مرور | ✅ "This phone number is not registered..." |
| رقم مسجّل + كلمة مرور خاطئة | ✅ "Incorrect password. Please try again." |
| رقم مسجّل + كلمة مرور صحيحة | ✅ دخول ناجح، جلسة محفوظة صح |
| تسجيل خروج ثم محاولة دخول تاني | ✅ الجلسة اتمسحت، وأمكن الدخول تاني بنجاح |
| إغلاق/فتح المتصفح (محاكاة) بعد تسجيل الدخول | ✅ الجلسة فضلت محفوظة تلقائيًا |
| حذف الحساب من قاعدة البيانات أثناء وجود جلسة | ✅ اتسجّل خروج تلقائي خلال ثانية ونص |

---

## الملفات اللي اتعدّلت
**`app.js` فقط** — إضافة 3 دوال جديدة (`findAccountByPhone`, `verifyAccountPassword`,
`verifySessionInBackground`)، وإعادة كتابة `handleLogin` و`handleRegister` لاستخدامها بدل
المنطق القديم المخلوط. **مفيش أي تغيير في التصميم أو الواجهات** — نفس الفورم، نفس الحقول،
نفس الأزرار بالظبط، التعديل كله في المنطق البرمجي خلف الكواليس.
